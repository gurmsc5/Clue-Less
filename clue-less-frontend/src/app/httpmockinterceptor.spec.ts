import { Httpmockinterceptor } from './httpmockinterceptor';

describe('Httpmockinterceptor', () => {
  it('should create an instance', () => {
    expect(new Httpmockinterceptor()).toBeTruthy();
  });
});
